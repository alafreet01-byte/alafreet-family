# حالة مشروع ALAFREET Family OS

## الحالة الحالية

- البناء الكامل ناجح على Next.js 15.
- تسجيل الدخول يعمل باسم المستخدم.
- حساب خليفة مربوط بصلاحية `super_admin`.
- حساب أحمد مربوط كطالب في Grade 10 ونظام Toddle.
- لوحة إدارة الحسابات مرتبطة بـ Supabase Auth و`family_members`.
- صفحات الأب والإدارة والمحرك محمية حسب الدور.
- الحسابات الجديدة تحصل على بريد داخلي بصيغة `username@alafreet.ae`.
- صفحة «قصتنا» موجودة بدل رابط 404 السابق.
- مفتاح Supabase السري موجود في `.env.local` فقط ولا يدخل النسخة الاحتياطية.

## التشغيل

استخدم زر `START_ALAFREET_ADMIN` الموجود على سطح مكتب Windows، واترك نافذة الخادم مفتوحة أثناء الاستخدام.

## الحسابات المرتبطة

| الشخص | اسم المستخدم | الدور | الحالة |
|---|---|---|---|
| خليفة | `khalifa` | `super_admin` | مربوط |
| أحمد | `ahmed` | `student` | مربوط |

بقية أفراد العائلة موجودون في قاعدة البيانات لكن حسابات دخولهم لم تُنشأ بعد.
